(function() {
  // Background script intentionally stays as a no-op shell for the bundled GeckoView extension.
  // Interface density is implemented through the WebView HTML viewport path; Gecko tabs.setZoom is page zoom.
})();
